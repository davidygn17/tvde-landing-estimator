[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3.12 -m pip install -r requirements.txt"]

[start]
cmd = "cd src && python3.12 -m uvicorn tvde_qr.main:app --host 0.0.0.0 --port $PORT"